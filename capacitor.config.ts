import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'photoviewer-issue-repro',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
